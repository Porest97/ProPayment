@model IEnumerable<ProPayment.Models.RefereeFee>

@{
    ViewData["Title"] = "Index";
}

<h1>Doamr Ersättningar</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table-striped table-responsive-xl" id="RefereeFeeList">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RefereeFeeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RefereeFeeCategory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RefereeFeeValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RefereeFeeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RefereeFeeCategory)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RefereeFeeValue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
